import React from 'react'

const other = () => {
  return (
    <div>
        {/* <h1>Websocket Chat App Test</h1>
      {isLoggedIn && <p>You are currently logged in as: {user.username}</p>}
      <form onSubmit={handleLoginSubmit}>
        <p>State your username</p>
        <label htmlFor="username">Username</label>
        <input onChange={handleLoginChange} name="username"/>
        <button type="submit">Log In</button>
      </form>
      <div>
        <form onSubmit={handleMessageSubmit} name="sendMessage">
          <label htmlFor="typeMessage">Type in a message</label>
          <input onChange={handleMessageChange} name="typeMessage"></input>
          <button type="submit">Send message</button>
        </form>
      </div>
      <div>
        {messages.map((message) => <p>{message}</p>)} */}
    </div>
  )
}

export default other